https://note.com/yukinouz1/n/n6fd6b6a544b3
上記サイトを参考に
環境構築行ってみました。

・ベンダープレフィックスを付けたり
・cssのプロパティをアルファベット順にしたり
結論
vscのみでは自動化出来ない事を自動化するツール？のように触っていて感じました。

コーディング→vscで打つ
ベンダープレフィックスを付与等→環境構築の際に導入したパッケージ等を用いる。

（環境構築の際パッケージをローカルインストールしたフォルダを開いて
所定のフォルダをでindex.html作成）
（ターミナルでコマンドを入力すると、それがトリガーになって設定していたパッケージを
用いた後の結果が出来る。例：ブラウザ表示、ベンダープレフィックス付与とか）

ただ普通にwebサイトを作成するのと違い、完成形というか「何をつくってるか」が見えにくい。
環境構築の結果どうなるというか。
「作った結果～したら～できる」ようになったが毛色が違うというか分かりにくいですね・・。

・もう少しローカルで「動き」というか「有用性？」がわかる参考がないか見てみます。

・どうも初めはフォルダわけとかせず、（srcフォルダとかdistフォルダとか作らず）
npm install パッケージでパッケージを集めて、何かして、bin等を起動させてvscのようなアプリ
になる感じかとイメージしていました。
ですか作る過程で下記のように感じた為
（パッケージをインストール、使えるように設定（動き）をコーディング、その過程で必要なフォルダを作成、動きを見る為
のhtmlファイルなどを作成・・・コマンドを打ってコーディングした動きを起こす。・・・その動きで
便利に使える。また、この「パッケージ」「設定」は共有できる。パッケージはnpmで管理。設定はファイルに書いてるものだから。）














